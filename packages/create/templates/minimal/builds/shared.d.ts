declare const GLOBAL_KEY: unique symbol;
declare namespace process {
    namespace env {
        let SHELL: string;
        let LSCOLORS: string;
        let USER_ZDOTDIR: string;
        let COLORTERM: string;
        let LESS: string;
        let NVM_INC: string;
        let WSL2_GUI_APPS_ENABLED: string;
        let TERM_PROGRAM_VERSION: string;
        let WSL_DISTRO_NAME: string;
        let NODE: string;
        let npm_config_local_prefix: string;
        let NAME: string;
        let PWD: string;
        let LOGNAME: string;
        let _: string;
        let VSCODE_GIT_ASKPASS_NODE: string;
        let VSCODE_INJECTION: string;
        let HOME: string;
        let LANG: string;
        let WSL_INTEROP: string;
        let LS_COLORS: string;
        let WAYLAND_DISPLAY: string;
        let GIT_ASKPASS: string;
        let NVM_DIR: string;
        let VSCODE_GIT_ASKPASS_EXTRA_ARGS: string;
        let TERM: string;
        let npm_package_name: string;
        let ZSH: string;
        let ZDOTDIR: string;
        let USER: string;
        let VSCODE_GIT_IPC_HANDLE: string;
        let DISPLAY: string;
        let npm_lifecycle_event: string;
        let SHLVL: string;
        let NVM_CD_FLAGS: string;
        let PAGER: string;
        let npm_config_user_agent: string;
        let npm_execpath: string;
        let XDG_RUNTIME_DIR: string;
        let npm_package_json: string;
        let WSLENV: string;
        let BUN_INSTALL: string;
        let VSCODE_GIT_ASKPASS_MAIN: string;
        let PATH: string;
        let DBUS_SESSION_BUS_ADDRESS: string;
        let NVM_BIN: string;
        let HOSTTYPE: string;
        let PULSE_SERVER: string;
        let npm_node_execpath: string;
        let OLDPWD: string;
        let TERM_PROGRAM: string;
        let VSCODE_IPC_HOOK_CLI: string;
        let PORT: string;
        let MINIFY: string;
    }
}
